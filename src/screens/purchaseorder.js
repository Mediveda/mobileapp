import { View, Text } from 'react-native';
import { screensEnabled } from 'react-native-screens';

const Purchase = () => (
    <View>
      <Text>purchase order screen</Text>
    </View>
  );
  export default Purchase;
  